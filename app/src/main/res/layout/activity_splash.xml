<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#16232A">

    <!--
        Custom logo (dari file picker user) — ditampilkan di tengah,
        proporsional, max 50% lebar layar agar pas di TV maupun phone.
        Visibility diatur via Java: VISIBLE jika ada custom logo, GONE jika tidak.
    -->
    <ImageView
        android:id="@+id/iv_custom_splash_logo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:padding="48dp"
        android:visibility="gone"
        android:contentDescription="Custom splash logo"/>

    <!--
        Default: "Play\nM3U." teks splash.
        alpha diatur via Java sebelum animasi (mulai 0f).
    -->
    <LinearLayout
        android:id="@+id/splash_text_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <!-- Baris 1: "Play" -->
        <TextView
            android:id="@+id/tv_splash_play"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Play"
            android:textColor="#E4EEF0"
            android:textSize="80sp"
            android:fontFamily="@font/inter_black"
            android:lineSpacingMultiplier="0.9"
            android:includeFontPadding="false"/>

        <!-- Baris 2: "M3U" + titik oranye -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="bottom">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="M3U"
                android:textColor="#E4EEF0"
                android:textSize="80sp"
                android:fontFamily="@font/inter_black"
                android:includeFontPadding="false"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="."
                android:textColor="#FF5B04"
                android:textSize="80sp"
                android:fontFamily="@font/inter_black"
                android:includeFontPadding="false"/>

        </LinearLayout>
    </LinearLayout>

</FrameLayout>
